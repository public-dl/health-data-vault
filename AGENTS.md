# Health Data Vault — 作業指針

## 目的と作業範囲

自治体等が公開する健康関連データを、出典・対象年度・定義を追跡できる公共データアーカイブとして収集・整理する。実装前に `docs/data_inventory.md` の調査結果と未解決事項を確認する。

現在は初期のデータ調査段階。Webサイトやアプリの作成、データの大量保存、公開・デプロイ、commit・pushは、ユーザーが次の作業として明示的に依頼するまで行わない。調査結果と実装案を混同しない。

## データの意味と品質

- 公式公開データとその年度に適用される公式の定義・注記を優先する。推測を確認済みの事実として記載しない。
- 元データの意味を拡張しない。健診の受診者数、判定人数、構成割合、がん検診の発見率等を、住民全体の疾病有病率・罹患率に読み替えない。
- 母集団、集計対象範囲、分子・分母、単位、判定閾値、年齢・性別、検査方法、服薬者の扱い、報告時点を保持する。不明な定義は未確認と明記し、必要な比較・公開を保留する。
- 掲載・刊行年度、実績年度、暦年／年度、精密検査の対象コホート年度、把握時点を別々に管理する。ファイル名だけで実績年度を決めない。
- 定義・母集団・単位が異なる年度を安易に時系列接続しない。同名の指標でも比較可能性を検証する。比較不可の区間は系列を分け、理由を表示する。
- 欠損値を0にしない。空欄、文字の `-` / `－`、`・`、未実施、未把握、判定不能、Excelエラー、真の数値0を区別する。表示形式で0がハイフンに見える場合は、セルの型・実値・表示形式を確認する。
- 割合の単純平均や合計を行わない。再集計は同一定義の分子・分母が揃う場合だけ行い、計算式と丸め規則を記録する。分母0と分母不明は計算不可とする。
- 県計、保健所計、市町村計、男女計、年齢計、再掲、初診・再診の合計を重複加算しない。重複行は自動削除せず、採否と根拠を記録する。別々の周辺集計から市町村×性別×年齢の交差表を推定しない。

## 原本と加工データ

- 元Excel・PDF等はバイト列のまま保存し、編集・再保存・再計算・上書きしない。マクロや外部リンク更新を実行しない。
- 将来の取得時は `data/raw/` と `data/processed/` を分離する。調査用一時ファイルも成果物と分ける。加工・修正は派生データまたは変換規則にのみ適用する。
- raw取得ごとに、発行主体、掲載ページURL、直接取得URL、取得日時（タイムゾーン付き）、元ファイル名、サイズ、SHA-256、取得できたHTTP更新情報を保持する。同じURLの差し替えも別版として扱う。
- 各加工レコードから原本の版・シート名・セル／範囲・表番号／PDFページ・注記へ遡れるようにする。原値と変換後の値、変換理由、変換コードの版を保持する。
- rawや大容量加工物を無断でGitに追加しない。データ保存を実装するときは、最初に保存先・容量・Git除外・配布方針を決める。資格情報や個人情報を保存しない。

## 定義・構造変更の検知

- 年度ごとにシート名（前後の空白を含む）、表題、多段見出し、結合範囲、データ型、年齢区分、単位、注記、分母、判定基準の版を比較する。
- 固定行・列番号やURLの年度置換だけに依存しない。掲載ページの実リンクを収集し、指標辞書・年度別マッピングで意味を確認する。
- 想定外の列・単位・対象者・定義の変更や未知の欠損記号は、黙って吸収せず検証エラーまたは要確認として扱う。
- 構造の一致と定義の一致を別々に判定する。判定基準の変更履歴と時系列接続の可否・根拠を管理する。

## 公開前の検証

- 取得、原本検査、抽出、標準化、検証、公開を分離し、同じ原本と設定から再現できる構造にする。
- 地域コード・年度・指標・性別・年齢・検査方法等のキーの一意性、重複、欠損、数値範囲、単位、分子分母、公式計との整合を検証する。
- Excelの数式と保存済み計算値を区別する。原本のエラーや不整合は元ファイルを修正せず課題として記録し、関連する公開値の採否を決める。
- 年度別の代表値を公式表と照合し、定義変更・重複・欠損・ゼロ分母・単位混在を検出する意味のあるテストを用意する。
- 検証結果、例外、採用した値の根拠をレビュー可能にする。未解決の重大な不整合を含む指標は公開対象から外す。
- 公開画面・API・ダウンロードには、出典URL、対象年度、母集団、単位、定義、取得日、比較上の制限を付ける。再利用条件・出典表示条件も公開前に確認する。

## 作業報告

作成・変更したファイル、調査・検証の範囲、確認済みの事実、未確認事項、データの保存先を簡潔に報告する。commit・push・公開は実施した場合だけ実施済みと記載する。
