# 初期表示・全区分地図マトリクス（2026-09-19）

- 初回ロード・条件クリア：メタボ全区分、割合、県計、地域Bなし。メタボグループがない旧スキーマは既存の先頭指標へフォールバック。
- 1地域：4カテゴリーの2×2 small multiples。2地域：カテゴリー行×A/B列。公開グループ定義の順（基準該当、予備群、非該当、判定不能）を使用。
- 左右／上下切替は全カテゴリーに共通。上下では各行A→B。狭幅は既存レスポンシブによる上下配置。
- 各行で同一categoryContextを共有し、単位・年度・色・階級を一致させる。データ・定義・計算・Validatorは変更しない。
- 比較コピー／PNGは各カテゴリー行の2地域を出力。DOM取得を行スコープに限定し、他カテゴリーとの混同を防ぐ。地域別出力も維持。

## 確認

75 Vitestテスト成功、production build成功。
実ブラウザーで初回ロードの全区分/rate/県計/Bなし、8地図化、4行順、全行のA/B凡例一致を確認。
A/B入替（長岡市→県計）、2022年度、人数への切替を全8地図のDOM属性で確認。
上下表示、カテゴリー行の比較PNG保存、B解除で4地図2×2、条件クリアを確認。
単区分へ切替時は2地図・マトリクスなし。PC1440pxの左右配置を目視確認。
commit・pushなし。

変更：web/src/main.tsx、web/src/group-views.tsx、web/src/comparison-export.tsx、web/src/style.css、本書。
