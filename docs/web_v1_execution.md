# 第1弾Web UI 実装・検証報告

確認日：2026-09-18。外部デプロイ、公開承認、commit、pushは実施していない。現在表示できるものは、検証を通過した未承認候補のローカルプレビューである。

## 使用したデータと公開候補

- 入力run：`44595cc6bf204cffa81c485d72f91019`
- 実績年度：2021・2022・2023（R3・R4・R5）。掲載年度：2022・2023・2024。
- 母集団：既存仕様の市町村国保・40～74歳の特定健診報告対象。全住民・全保険者の疾病有病率ではない。
- 候補ID：`aaaff5ce3946199872c319c5de23a6b1c882afe0d1213000a37f646e27981f4b`
- 県計＋30市町村、8指標×3年度×31地域＝744レコード。今回の選択範囲には欠損なし。単位はすべて人数。
- 候補と検証結果は `data/public/candidates/<候補ID>/`。原本、加工物、地図、候補はGit管理外の `data/` に保存。
- `data/public/current.json` は未作成。自動取得・候補生成は公開参照先を変更しない。

| 指標ID | 公開候補の表示名 |
|---|---|
| metabo_case | メタボ判定：基準該当 |
| metabo_preliminary | メタボ判定：予備群 |
| metabo_noncase | メタボ判定：非該当 |
| metabo_indeterminate | メタボ判定：判定不能 |
| guidance_active | 保健指導レベル：積極的支援 |
| guidance_motivational | 保健指導レベル：動機付け支援 |
| guidance_none | 保健指導レベル：なし |
| guidance_indeterminate | 保健指導レベル：判定不能 |

保健指導の人数は対象選定レベルであり、指導の実施・終了人数ではない。「判定不能人数」は数値として保持し、欠損記号とは区別する。指標設定は `hdv/public_config/indicators.json` に分離し、UIは候補の指標一覧を使用する。将来の追加には元の定義・比較区間・構造の検証も必要で、設定だけでpendingを昇格させることはできない。

## 実装した機能

- 原本からの再検証、公開候補の決定的生成、内容ハッシュ、公開Validator、明示的な承認、承認済み版の静的エクスポート。
- 公開時は承認済みreleaseを参照。ブラウザーでも承認状態とハッシュを確認し、processed/latest.jsonを読まない。
- 市町村分布図、選択市町村の強調表示、年度切替・再生、県計／自治体の表と折れ線、系列の重ね合わせ。
- 人数表示、出典セル・原本ハッシュ・取得日時・対象年度・定義・制限を含む諸元モーダル。
- 表のコピー／CSV、地図・表・グラフのPNG、画像コピー。Clipboard APIの可否はブラウザーに依存し、失敗時は明示する。
- 検証値に基づく定型説明。根拠レコードを保持し、生成AI APIや個人の健康判断は使用しない。
- 年度ページ発見・原本取得・既知契約の検証・候補生成の監視CLI。承認処理とは分離。

## テスト・Validator結果

| 確認 | 結果 |
|---|---|
| Python unittest | 39件成功、スキップ0 |
| フロントエンド Vitest | 15件成功 |
| TypeScript／Vite build | 成功 |
| 既存Validator再実行 | エラー0、警告72件 |
| 県計回帰検証 | 123セル一致 |
| 公開Validator（別プロセスでも再検証） | エラー0、744レコード、8指標、31地域 |

欠損と0、比較区間、未検証・pendingの拒否、キー重複、設定／候補改変、ハッシュ・承認者・再利用条件確認、監視による公開切替禁止などをテストした。ブラウザーでは年度・指標・自治体切替、重ね合わせ、出典モーダル、表コピー、PNG保存の完了表示、390px幅の表示を確認した。ダウンロードしたPNGファイル自体の再読込検証はしていない。

実値例としてメタボ基準該当の県計22,401／22,419／21,934人、長岡市2,303／2,620／2,594人（2021→2023）を確認。通常URLは承認済みデータなしのエラー表示となり、候補を自動表示しないことも確認した。

## 除外・保留と残る制約

- 元の採用データ中pending 4,320件、incompatible 135件は原値を保持したままWeb候補から除外。保健所計・市計・町村計も地域重複加算を避けるためWeb候補に含めない。
- 元データの新潟市重複除外123件、対象外の国保組合328件の採否記録は既存パイプラインに保持。
- 入力全体の警告72件は解消したものとして扱わない。公開候補の検証結果にも入力警告があることを記録する。
- 国土数値情報N03の2023年1月1日版を全年度の参考境界として使用。新潟市8区を市へ統合。年度別境界変化は未検証、地区別データなし。形状簡略化は隣接自治体間の共有境界を一括処理していないため、微細な隙間が生じる可能性がある。
- 地理データの再利用条件・必要な許諾は公開前確認待ち。具体的出典と手順は `publication_workflow.md` を参照。確認済みと偽って承認フラグを付けない。
- 率、平均、性別・年齢階級、NDB、生成AI APIは実装していない。
- スケジューラー登録・通知連携・GitHub Actions・外部ホスティングは未実施。監視CLIの新年度は構造契約と定義のレビューが必要。今回の監視検証は模擬取得による自動テストで、定期監視を稼働させたものではない。
- 承認CLIの名前は監査記録であり認証・電子署名ではない。実運用では実行環境の権限を分離する。

## ローカル確認

現在の確認URL：`http://127.0.0.1:4174/?review=1`。停止後はREADMEの依存導入・Webビルドを実施し、リポジトリルートで次を実行する。

```powershell
.venv\Scripts\python -m hdv.serve --review-release aaaff5ce3946199872c319c5de23a6b1c882afe0d1213000a37f646e27981f4b --port 4174
```

## 作成・変更ファイル

変更：`.gitignore`、`README.md`、`pyproject.toml`。

作成：

- `hdv/geography.py`、`hdv/insights.py`、`hdv/monitor.py`、`hdv/public_validator.py`、`hdv/publisher.py`、`hdv/serve.py`
- `hdv/public_config/indicators.json`、`tests/test_publication.py`
- `web/package.json`、`web/pnpm-lock.yaml`、`web/pnpm-workspace.yaml`、`web/tsconfig.json`、`web/index.html`
- `web/src/main.tsx`、`web/src/model.ts`、`web/src/model.test.ts`、`web/src/export.ts`、`web/src/style.css`
- `docs/public_data_model.md`、`docs/publication_workflow.md`、`docs/web_v1_execution.md`

既存AGENTS、調査仕様、Collector／Normalizer／Validatorは変更していない。作業前から存在した未追跡の `docs/ndb_healthcheck_inventory.md` と `ダウンロード.png` は今回の作成物ではない。生成データ・依存ライブラリ・ビルド出力はGit除外し、ステージングも行っていない。


## 2026-09-19 受診者構成割合の実装追記

先行の人数のみの報告は上記の時点記録。現在の確認URLは `http://127.0.0.1:4175/?review=1`。

- 新候補：`9cc746ea039f58b564ce8a3ec1b5bfee653181ec2897c6bc6e0277f2eadd3945`
- 原値744件を維持し、派生割合744件・分母93件を追加。公開Validatorの別プロセス再検証はエラー0。入力再検証もエラー0、既存警告72件、県計123セル一致。
- 初期表示は受診者に占める割合（%）。人数に切替可能。地図・グラフ・見どころ・CSV/コピー/PNGが選択単位に連動。表は報告人数／受診者数を併記。
- ブラウザーで割合初期表示、人数切替、長岡市の選択、別指標・別年度、分子E28／分母B28を含む出典モーダルを確認。
- 割合の年度比較は人数と独立した定義版で管理。年齢・性別調整はない。再利用条件や2023年固定境界等、従来の公開前制約は継続。
- public/current.jsonは未作成。公開承認・外部公開・commit・pushは実施していない。

再起動（リポジトリルート、依存導入後）：

```powershell
.venv\Scripts\python -m hdv.serve --review-release 9cc746ea039f58b564ce8a3ec1b5bfee653181ec2897c6bc6e0277f2eadd3945 --port 4175
```

今回追加：`hdv/rates.py`、`tests/test_rates.py`、`docs/recipient_percentage_audit.md`。
今回更新：`hdv/publisher.py`、`hdv/public_validator.py`、`hdv/insights.py`、`hdv/public_config/indicators.json`、`web/src/model.ts`、`web/src/model.test.ts`、`web/src/main.tsx`、`web/src/style.css`、`README.md`、`docs/public_data_model.md`、この実行報告。

最終確認：Python unittest 47件成功（スキップ0）、Vitest 19件成功、TypeScript／Viteビルド成功。分母0・欠損・不一致・母集団違い・改変・比較判定の独立性を含む。


## 2026-09-19 地図・表のレイアウト切替

- 変更ファイル：`web/src/main.tsx`、`web/src/style.css`、この実行報告。
- 市町村選択時に地図・表それぞれ独立した「左右比較／上下表示」を表示。初期選択は左右比較、画面幅1100px未満は自動で上下配置。画面幅を戻すと選択した表示へ戻る。
- レイアウトは表示専用の状態で、単位・年度・市町村・指標を変更しても保持する。DOMの同じパネルをCSSで並べ替え、データや公開候補を変更しない。
- PC左右表示は地図のサイズ・投影・凡例を共通にし、表の見出し高さと行の高さを揃えた。グラフは従来の上段重ね合わせ＋下段単独のまま。
- 県計との差の新しい補助列は今回追加しない。既存の情報と出力を維持したまま、同年度の横比較を優先した。
- 代表ケース：長岡市、メタボ基準該当、受診者に占める割合。1440px幅で左右各627px・同一上端、表の3年度の行上端が左右とも一致。上下切替時は各1272pxで県→市の順。
- 390px幅で左右選択のまま自動上下配置、ページ全体の横はみ出しなし。表は従来どおり内部横スクロールで列を保持。
- 人数切替・2021年度・新潟市への変更後も地図と表の個別レイアウトを保持し、県計22,401人・新潟市7,432人の表示を確認。長岡市・割合へ戻した後も値と分母が対応。
- 表コピー、CSV、PNGボタンで完了表示を確認。ブラウザーのクリップボード読取ではテキストを取得できず、保存ファイル自体の再読込検証はしていない。CSV/TSVの内容・出典は既存自動テストで確認。
- グラフの選択市町村系列をOFFにすると上段3点（県計のみ）、グラフパネルは上下2枚を維持。地図の左右凡例は一致。
- TypeScript/Viteビルド成功、Vitest 19件成功。公開Validator再実行はエラー0（人数744、派生割合744、分母93）。候補IDは従来の9cc746ea039f…のまま。元の警告72件も維持。
- データ定義・派生計算・comparability・provenance・公開承認ロジックは変更していない。公開承認、commit、pushは行っていない。

## 2026-09-19 任意2地域比較への拡張

この節は上記の「県計固定／グラフ2枚」の仕様を更新する。

### 実装

- 地域選択は `regions: string[]` と地域メタデータを使用。地域Aの初期値は県計、地域Bは任意。各31地域から選択でき、相手と同じ地域は無効化する。入替ボタン、B未選択の市町村単独閲覧に対応。
- 地図・表は配列順でA→Bを描画。個別の左右／上下切替を維持し、1100px未満は上下配置へ変換する。共通指標・年度・単位・凡例を使用。地区別データは生成しない。
- 表は人数・分母・年度・比較可否を維持。割合ではBに「地域Aとの差」を表示する。差は同年度・同定義・同母集団・同単位かつ比較可能な検証値のB−Aを丸め前に計算する。欠損や不一致は算出不可。
- グラフは常に1枚。選択地域ごとのチェックボックス、最低1系列の保護、色・実線／破線・丸／四角の点を使用。地域変更時は全選択系列をONへ戻す。単位・年度・レイアウト変更では地域選択を保持する。
- Y軸は表示ON/OFFと独立して全選択地域の値から決定。PNG／画像コピーには表示中の系列だけを描画し、地域名・指標・単位・年度範囲を含める。出典モーダルは非表示系列を含む全選択地域の原典を引き続き参照できる。
- 数値ラベルはCanvasで測定した文字幅から矩形を作り、既配置矩形との近接を検出して上下オフセットを段階的に拡大する。描画領域内へ制限し、引き出し線で元の点と対応させる。系列数に依存しない共通関数とし、5地域相当の衝突・端点を単体テスト。UIは2地域まで。
- 見どころは表示中の地域の検証値、連続して比較可能な年度間の差、同年度の地域差のみを説明。丸め前の差である旨を明記し、健康評価・原因・施策効果を推定しない。
- 指標一覧・group・割合設定は既存メタデータを維持。NDB、性別年齢別、カテゴリー一括、5地域選択は実装していない。

### 検証

- Vitest 31件成功（既存19＋比較・出典順序・差の条件・ラベル配置12）、TypeScript／Viteビルド成功。
- 既存Python unittest 47件成功、スキップなし。
- 既存Validator：エラー0、警告72、5986レコード。公開Validator：エラー0、人数744、派生割合744、分母93、8指標・31地域。
- 1440px幅：県計×長岡市の左右地図・表、三条市×燕市、三条市×加茂市、地域入替、単独閲覧を確認。燕市をAへ入替後、出典の2023年度分子793（E23）／分母3401（B23）、シート名・ハッシュ・URLが燕市に対応することを確認。
- 三条市×加茂市は2023年度の双方が表示上19.8%。6ラベルの描画矩形に重なりなし。390px幅でもラベルを分離、地図は自動上下配置、ページ全体の横はみ出しなし。スマートフォンのグラフ・表は内部横スクロールを維持し、グラフには案内を追加。
- 三条市×燕市で三条市OFF後もY軸上限26.581005586592177を保持。燕市だけの1系列となり、最後のチェックを無効化。地域入替後は両系列ON。
- 上下表示のまま人数・2021年度へ切替後も地域とレイアウトを保持（三条市1235人、加茂市185人）。年度再生でスライダーが更新されることを確認。
- グラフPNG・画像コピー、表CSV・コピーで完了通知を確認。保存されたPNG/CSVやOSクリップボードの内容の再読込は実施していない。描画DOMの系列限定、CSV/TSVの原値・出典はコードと既存テストで検証。

### 成果物と公開状態

今回追加：`web/src/comparison.ts`、`web/src/comparison.test.ts`。
今回変更：`web/src/main.tsx`、`web/src/style.css`、本書。

公開候補のSHA-256は `9cc746ea039f58b564ce8a3ec1b5bfee653181ec2897c6bc6e0277f2eadd3945` のまま。データ定義、派生割合計算、comparability、原本、公開承認処理は変更していない。`data/public/current.json` は未作成。commit・push・公開承認は実施していない。

確認URL：`http://127.0.0.1:4175/?review=1`。再起動は上記の `hdv.serve --review-release ... --port 4175` を使用する。候補データの明示的なローカルレビューであり、承認済み公開版ではない。

## 2026-09-19 全区分表示の実装

### 結果

- メタボ判定・保健指導レベルの両方を、同じグループモデル・UI部品で実装。
- 新候補：`f66a35326c7f394b31ef7a8e4fb46bdbddbd28701583248afd9d8edfda19ac17`、schema public-3。
- 人数744件・派生割合744件・分母93件は従来のpublic-2と同一。グループ2件と構成検証186件を追加。
- 単区分の初期値・任意2地域・入替・左右／上下・再生・折れ線・出典・人数／割合の仕組みを維持。既存パネルをpanels.tsxへ移して共用した。
- 地図一覧2×2／狭幅1列、一覧の地域切替、カテゴリーごとの地域比較、全区分表、構成積み上げ、1区分の折れ線への切替を実装。
- カテゴリー別の凡例境界を明示的な公開metadataへ保存。選択地域・年度では変えない。これは医学的な閾値ではない。
- 全区分地図PNG／コピー、横持ち表コピー、縦長CSV、構成グラフPNG／コピーへ対応。積み上げの細い区分も補助一覧に残す。
- 新グループ設定は候補内にスナップショット保存・ハッシュ検証。旧public-2候補の再検証も維持。

### テスト・実画面確認

- Python unittest：55件成功、スキップ0。グループ異常系、構成証跡改変、丸め、設定スナップショット改変、既存公開承認の回帰を含む。
- Vitest：38件成功。既存31件に、グループ順序・0・欠損・重複・丸め非補正・再正規化禁止・出力provenance等7件を追加。
- TypeScript／Viteビルド成功。
- 新public-3、既存public-2の公開Validatorを別々に原本再構築して検証。両方エラー0。原本の既存警告72件、県計回帰123セルは継続。
- 1440px：県計×長岡市でメタボの全区分表の各年度上端が左右完全一致。4地図一覧と地域比較を確認。保健指導も同じ部品で表示。
- 地域入替後、長岡市の2023年保健指導判定不能41人／14660人、分子O28・分母B28、原本ハッシュ・シート末尾空白・公開状態をモーダルで確認。
- 長岡市×新潟市の人数構成で、新潟市をOFFにしてもY軸40433.8を維持。3本×4区分＝12要素を保持し、判定不能の強調後も要素数は同じ。最後の地域OFFは不可。
- 「区分の推移」では1枚・2地域系列のみ。単区分へ戻しても地域A/B・単位・年度を維持。
- 390px：4地図が1列、全体の横はみ出しなし。構成グラフは内部横スクロール。棒をタップして年度・地域・4区分・受診者数・原典を確認。
- 820px：地図2列、4つのHTML凡例で文字サイズを確保し、ページの横はみ出しなし。
- ダウンロードした保健指導の4地図PNGと構成PNGを画像として再読込し、カテゴリー凡例、細い区分の補助値、年度・地域・単位・出典・release／未承認表示を確認。
- 長岡市の全区分CSVを再読込し12行、4指標ID、group_id=guidance、category_order=0〜3、2021〜2023年度、分母セルB28、分子分母のURL・SHAが保持されていることを確認。表コピーも完了通知を確認したがOSクリップボードの再読込はしていない。

### 変更ファイル（今回分）

追加：
- `hdv/groups.py`
- `hdv/public_config/groups.json`
- `tests/test_groups.py`
- `web/src/panels.tsx`（既存パネルの共用化）
- `web/src/group-model.ts`
- `web/src/group-model.test.ts`
- `web/src/group-views.tsx`
- `docs/group_views.md`

変更：
- `hdv/publisher.py`
- `hdv/public_validator.py`
- `tests/test_publication.py`
- `web/src/main.tsx`
- `web/src/model.ts`
- `web/src/style.css`
- `docs/public_data_model.md`
- `docs/web_v1_execution.md`
- `README.md`

Collector、Normalizer、既存の割合計算（rates.py）、原本、AGENTS.mdは変更していない。候補とビルド出力はGit除外されたdata/public/candidates/とweb/dist/に保存。実画面テストのPNG2件・CSV1件はブラウザー経由でユーザーのDownloadsへ保存された。大量データ・出力画像をGitへ追加していない。

### 公開状態・確認方法

`data/public/current.json` は未作成。公開承認・外部公開・commit・pushは実施していない。再利用条件の公開前確認、固定2023年境界、年齢性別未調整、将来年度の追加監査は継続制約。

起動中の確認URL：`http://127.0.0.1:4176/?review=1`。

```powershell
.venv\Scripts\python -m hdv.serve --review-release f66a35326c7f394b31ef7a8e4fb46bdbddbd28701583248afd9d8edfda19ac17 --port 4176
```

表示項目から「メタボ判定：全区分」または「保健指導レベル：全区分」を選択する。通常起動の初期値は従来の単区分。


## 2026-09-19 構成グラフの全値常時表示

- 変更：`web/src/group-views.tsx`、`web/src/style.css`、新規 `web/src/stack-labels.ts`・`web/src/stack-labels.test.ts`、本書、`docs/group_views.md`。
- 数値文字列の幅と区分高さから内部／外部配置を決定。外部ラベルはカテゴリー色の枠と引き出し線を付ける。全棒の矩形と全ラベルを衝突判定し、上下／左右移動、必要時にはグラフ下部の領域拡張で省略を防ぐ。
- 値一覧をグラフ直下へ常時併記。狭い画面は1列。既存のSVG内一覧も保持し、画像に全値と人数・分母を含める。割合の原値・丸め・計算・Validator・provenanceは変更していない。
- PC1440px／タブレット820px／スマートフォン390pxで確認。県計＋長岡市、2021〜2023年度、保健指導4区分の24ラベルを割合／人数で確認。人数表示およびメタボ割合表示のラベル矩形重複0。小区分は外部ラベル、人数表示で密集する場合は引き出し線が長くなることがある。スマートフォンでは常時一覧を読める。
- 長岡市2023年度：積極的支援1.8%（259人）、判定不能0.3%（41人）、分母14,660人を確認。2021・2022年度、県計の小区分も省略なし。
- PNGを実際に保存して画像内の全24値・凡例・出典を確認。コピー操作は成功通知を確認（クリップボードへの貼付先での再検証は未実施）。画像はDownloadsに保存し、Gitへ追加していない。
- Vitest 42件成功、TypeScript＋Viteビルド成功、Python unittest 55件成功。ゼロ／欠損・幅不足・隣接棒・高密度配置を追加テスト。
- 既存public-3候補 f66a35326c7f394b31ef7a8e4fb46bdbddbd28701583248afd9d8edfda19ac17 の公開Validator：errors=0。744人数・744割合・93分母・186構成は従来通り。入力警告72件、参考境界・未調整割合の制限も従来通り。
- commit・push・公開承認は未実施。


### メタボ判定のラベル再確認・欠落ファイル修復（2026-09-19）

再確認時、`group-views.tsx`とテストから参照する`web/src/stack-labels.ts`がファイルシステム上に存在しなかったため復旧し、再ビルドした。欠落理由は未確認。
県計の検証済み候補から取得したC6/D6/E6/F6の人数と分母を小規模な回帰フィクスチャとしてテスト内に記録。2021〜2023年度の4区分×3年度について割合・人数双方の全ラベル保持、矩形重複なし、描画範囲とフッターへの非侵入を検証。Vitest44件成功、TypeScript/Viteビルド成功。
実画面でもhoverなしの12ラベルと矩形重複0を確認。基準該当は19.1/19.3/19.3%、予備群9.5/9.3/9.1%、非該当70.9/71.0/71.1%、判定不能0.4/0.4/0.5%。PNG実出力を確認、画像コピーは成功通知を確認。配布データ・計算・Validatorは未変更。commit/push未実施。
今回の変更ファイル：`web/src/stack-labels.ts`、`web/src/stack-labels.test.ts`、本書。
