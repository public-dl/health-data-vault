import React,{useState} from 'react';
import {PublicHeader,PublicFooter} from './public-shell';
declare const __HDV_ORIGIN__:string;declare const __HDV_PRODUCTION__:boolean;
export async function sendContact(form:HTMLFormElement){const body=new URLSearchParams();new FormData(form).forEach((v,k)=>body.append(k,String(v)));const response=await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:body.toString()});if(!response.ok)throw new Error('submit failed');}
export function Contact(){const [state,setState]=useState('idle');const review=new URLSearchParams(location.search).has('review');const enabled=__HDV_PRODUCTION__&&location.origin===__HDV_ORIGIN__&&!new URLSearchParams(location.search).has('review');return <><PublicHeader review={review}/><main className="contact-page"><h1>お問い合わせ</h1><p>Health Data Vaultに関するご意見・お問い合わせをお送りいただけます。</p><form name="health-data-vault-contact" method="POST" data-netlify="true" data-netlify-honeypot="bot-field" onSubmit={async e=>{e.preventDefault();if(!enabled||state==='sending')return;const form=e.currentTarget;setState('sending');try{await sendContact(form);setState('success');form.reset();}catch{setState('error');}}}><input type="hidden" name="form-name" value="health-data-vault-contact"/><p hidden><label>入力しないでください<input name="bot-field" tabIndex={-1} autoComplete="off"/></label></p><label>お名前（任意）<input name="name" autoComplete="name" maxLength={100}/></label><label>メールアドレス（必須）<input name="email" type="email" required autoComplete="email" maxLength={254}/></label><label>お問い合わせ種別<select name="category">{['データについて','表示について','掲載内容について','その他'].map(s=><option key={s}>{s}</option>)}</select></label><label>お問い合わせ内容（必須）<textarea name="message" required rows={7} maxLength={5000}/></label><p>ご入力いただいた情報は、お問い合わせへの対応のために使用します。</p>{!enabled&&<p>この確認環境では送信できません。</p>}<button type="submit" disabled={!enabled||state==='sending'}>{state==='sending'?'送信中…':'送信する'}</button><p role="status">{state==='success'?'お問い合わせを受け付けました。':state==='error'?'送信できませんでした。時間をおいて再度お試しください。':''}</p></form></main><PublicFooter review={review}/></>;}
