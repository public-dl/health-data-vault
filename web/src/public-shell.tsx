import {appUrl} from './app-url';
import React from 'react';
export function PublicHeader({review=false,base}:{review?:boolean;base?:string}){const url=(path:string)=>appUrl(path,base);const q=review?'?review=1':'';return <header className="site-header"><a href={url('/')+q} className="brand"><span className="brand-bars" aria-hidden="true"><i/><i/><i/><i/></span><span>Health Data Vault<small>NIIGATA</small></span></a><nav aria-label="サイトナビゲーション"><a href={url('/')+q+'#map'}>地図</a><a href={url('/')+q+'#table'}>数表</a><a href={url('/learn/')+q}>特定健診を知る</a><a href={url('/tables/')+q}>公表数表</a><a href={url('/contact/')+q}>お問い合わせ</a></nav></header>;}
export function PublicFooter({review=false}:{review?:boolean}){const q=review?'?review=1':'';return <footer><strong>Health Data Vault</strong><span>公表データを、地域の理解へ。</span><nav aria-label="フッターナビゲーション"><a href={appUrl('/')+q+'#about-data'}>データについて</a><a href={appUrl('/learn/')+q}>特定健診を知る</a><a href={appUrl('/tables/')+q}>公表数表</a><a href={appUrl('/contact/')+q}>お問い合わせ</a></nav></footer>;}
